# YouTube Watch Later Clearer — Cookie Configuration
#
# How to get your cookie string:
# 1. Open Chrome and go to https://www.youtube.com/playlist?list=WL (must be logged in)
# 2. Open DevTools (F12) → Network tab → refresh the page
# 3. Click the first "playlist?list=WL" request
# 4. Under Request Headers → find "cookie:" and copy the entire value
# 5. Paste it below (a long string like: VISITOR_INFO1_LIVE=xxx; SID=xxx; ...)
#
# WARNING: This cookie string grants full access to your Google account.
#          Never share it or commit it to version control.

YT_COOKIES_RAW=

# Optional: delay between deletions in milliseconds (default: 500)
# DELETION_DELAY_MS=500
